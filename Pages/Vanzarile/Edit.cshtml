@page
@model PROIECT_SESIUNE_VINURI.Pages.Vanzarile.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Vanzari</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Vanzari.ID" />
            <div class="form-group">
                <label asp-for="Vanzari.UserID" class="control-label"></label>
                <select asp-for="Vanzari.UserID" class="form-control" asp-items="ViewBag.UserID"></select>
                <span asp-validation-for="Vanzari.UserID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Vanzari.VinID" class="control-label"></label>
                <select asp-for="Vanzari.VinID" class="form-control" asp-items="ViewBag.VinID"></select>
                <span asp-validation-for="Vanzari.VinID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
